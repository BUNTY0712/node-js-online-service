# Environment Configuration for Render.com Deployment
# Add these environment variables in your Render dashboard

NODE_ENV=production
PORT=10000
DB_URI=your_mongodb_atlas_connection_string
JWT_SECRET=your_super_secret_jwt_key_here_generate_new_one_for_production
DEV_MODE=false

# IMPORTANT: Replace with your actual Render app URL
# This URL is used for:
# 1. Cron job keep-alive pings
# 2. Generating full image URLs that work across all devices
API_URL=https://node-js-online-service.onrender.com

# Alternative: You can also use your custom domain if you have one
# API_URL=https://your-custom-domain.com
